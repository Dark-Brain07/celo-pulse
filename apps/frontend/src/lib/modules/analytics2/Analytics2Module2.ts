import { IAnalytics2Module2 } from "../../../types/modules/analytics2/IAnalytics2Module2";

export class Analytics2Module2 implements IAnalytics2Module2 {
  public id = Math.random().toString(36).substring(2, 9);
  public isActive = false;
  public createdAt = Date.now();
  public metadata: Record<string, any> = {};
  
  public init() {
    this.isActive = true;
    this.metadata['initializedAt'] = Date.now();
  }
  
  public destroy() {
    this.isActive = false;
  }
}
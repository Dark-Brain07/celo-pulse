import { IStateWallet3Module17 } from "../../../types/modules/state-wallet3/IStateWallet3Module17";

export class StateWallet3Module17 implements IStateWallet3Module17 {
  public id = Math.random().toString(36).substring(2, 9);
  public isActive = false;
  public createdAt = Date.now();
  public metadata: Record<string, any> = {};
  
  public init() {
    this.isActive = true;
    this.metadata['initializedAt'] = Date.now();
  }
  
  public destroy() {
    this.isActive = false;
  }
}
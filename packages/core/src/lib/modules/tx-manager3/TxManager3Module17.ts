import { ITxManager3Module17 } from "../../../types/modules/tx-manager3/ITxManager3Module17";

export class TxManager3Module17 implements ITxManager3Module17 {
  public id = Math.random().toString(36).substring(2, 9);
  public isActive = false;
  public createdAt = Date.now();
  public metadata: Record<string, any> = {};
  
  public init() {
    this.isActive = true;
    this.metadata['initializedAt'] = Date.now();
  }
  
  public destroy() {
    this.isActive = false;
  }
}
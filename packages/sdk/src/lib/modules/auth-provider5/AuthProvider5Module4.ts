import { IAuthProvider5Module4 } from "../../../types/modules/auth-provider5/IAuthProvider5Module4";

export class AuthProvider5Module4 implements IAuthProvider5Module4 {
  public id = Math.random().toString(36).substring(2, 9);
  public isActive = false;
  public createdAt = Date.now();
  public metadata: Record<string, any> = {};
  
  public init() {
    this.isActive = true;
    this.metadata['initializedAt'] = Date.now();
  }
  
  public destroy() {
    this.isActive = false;
  }
}
import { ITxManager5Module38 } from "../../../types/modules/tx-manager5/ITxManager5Module38";

export class TxManager5Module38 implements ITxManager5Module38 {
  public id = Math.random().toString(36).substring(2, 9);
  public isActive = false;
  public createdAt = Date.now();
  public metadata: Record<string, any> = {};
  
  public init() {
    this.isActive = true;
    this.metadata['initializedAt'] = Date.now();
  }
  
  public destroy() {
    this.isActive = false;
  }
}